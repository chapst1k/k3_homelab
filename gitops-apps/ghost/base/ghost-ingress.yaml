apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ghost-server-ingress
  namespace: ghost
spec:
  ingressClassName: traefik
  rules:
    - host: zmartin.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ghost
                port:
                  number: 80
