apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - name: glance-config
    files:
      - config/config.yaml
      - config/proxmox.yml
      - config/unifi.yml

generatorOptions:
  disableNameSuffixHash: true
