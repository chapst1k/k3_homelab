apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: glance
  namespace: glance
  
spec:
  tls:
    - hosts:
        - glance.zmartin.me
      secretName: wildcard-zmartin-me-tls
  rules:
    - host: glance.zmartin.me
